<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/background_color"
    tools:context=".ui.messages.FindContactActivity">

    <EditText
        android:id="@+id/contactSearchET"
        android:layout_width="256dp"
        android:layout_height="wrap_content"
        android:layout_marginStart="36dp"
        android:layout_marginLeft="36dp"
        android:layout_marginTop="64dp"
        android:background="@drawable/rounded_box"
        android:ems="10"
        android:hint="Handle"
        android:inputType="textPersonName"
        android:padding="20dp"
        android:textColor="@color/text_color"
        android:textColorHint="@color/text_color_hint"
        android:textSize="20sp"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <ImageView
        android:id="@+id/search_icon"
        android:layout_width="48dp"
        android:layout_height="48dp"
        android:onClick="findQuery"
        app:layout_constraintBottom_toBottomOf="@+id/contactSearchET"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toEndOf="@+id/contactSearchET"
        app:layout_constraintTop_toTopOf="@+id/contactSearchET"
        app:srcCompat="@drawable/ic_baseline_search_24"
        tools:targetApi="lollipop" />

    <ListView
        android:id="@+id/groupQueryList"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:layout_marginTop="32dp"
        android:background="@color/background_color"
        android:visibility="visible"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@+id/contactSearchET">

    </ListView>
</androidx.constraintlayout.widget.ConstraintLayout>